node_modules/
.DS_Store
*.tmp
fixtures/.tmp-selftest/

# audits/ is generated by checker/shape-diff.mjs on every run. It is not committed,
# so the report a reader sees is the one their own run produced.
audits/
